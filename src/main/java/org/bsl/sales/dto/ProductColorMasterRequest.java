package org.bsl.sales.dto;

/**
 * Legacy compatibility type retained only so an in-place source-folder replacement
 * cannot leave an older DTO in an inconsistent state. It is not used by active APIs.
 */
@Deprecated(forRemoval = true)
public record ProductColorMasterRequest() {
}
